// Lee County (Cape Coral / Fort Myers) motivated-seller leads — POPULATED BY scrape_fl_county.py
// (config: lee). Same lead schema as Polk (window.POLK_LEADS) + parcel-centroid lat/lng for the
// crime-band overlay. Empty until the scrape runs. Lee is the SW-Florida fix-and-flip market
// (high volume, mid price points, post-hurricane distress inventory).
window.LEE_LEADS=[];
window.LEE_META={county:'Lee',count:0,snapshot:'',markets:[]};
